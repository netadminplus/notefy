apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - deployment.yaml
  - postgres.yaml
  - ingress.yaml
  - secrets.yaml
  - backup-cronjob.yaml

# Modern way to handle labels
labels:
  - includeSelectors: true
    pairs:
      app: notefy
      environment: production

images:
  - name: ramtiin/notefy
    newTag: production-cc8caf6ebc2b80545b7520be78e68c761a102809

# Modern way to handle patches (fixes the "unique target" error)
patches:
  - target:
      kind: Deployment
      name: notefy
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 3
  - target:
      kind: PersistentVolumeClaim
      name: postgres-pvc
    patch: |-
      - op: replace
        path: /spec/resources/requests/storage
        value: 10Gi