apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - deployment.yaml
  - postgres.yaml
  - ingress.yaml
  - secrets.yaml
  - backup-cronjob.yaml

# Modern way to handle labels
labels:
  - includeSelectors: true
    pairs:
      app: notefy
      environment: production

images:
  - name: ramtiin/notefy
    newTag: production-d2f52e92213c6d9d42dcdfcbd4d526eb383e22e0

# Modern way to handle patches (fixes the "unique target" error)
patches:
  - target:
      kind: Deployment
      name: notefy
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 3
  - target:
      kind: PersistentVolumeClaim
      name: postgres-pvc
    patch: |-
      - op: replace
        path: /spec/resources/requests/storage
        value: 10Gi